{
  "name": "baleia",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Strategy brain on top of krill: capture → distill → plan → triage → execute.",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.mjs",
    "loader": "node --env-file-if-exists=.env src/persona-loader.mjs",
    "test": "node --test"
  }
}
